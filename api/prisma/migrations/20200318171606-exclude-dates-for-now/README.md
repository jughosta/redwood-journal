# Migration `20200318171606-exclude-dates-for-now`

This migration has been generated by JuliaRechkunova at 3/18/2020, 5:16:06 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Entry" (
    "answer" TEXT NOT NULL DEFAULT '' ,
    "chapterId" TEXT NOT NULL  ,
    "id" TEXT NOT NULL  ,
    "question" TEXT NOT NULL DEFAULT '' ,
    PRIMARY KEY ("id"),FOREIGN KEY ("chapterId") REFERENCES "Chapter"("id") ON DELETE RESTRICT ON UPDATE CASCADE
) 

INSERT INTO "quaint"."new_Entry" ("answer", "id", "question") SELECT "answer", "id", "question" FROM "quaint"."Entry"

DROP TABLE "quaint"."Entry";

ALTER TABLE "quaint"."new_Entry" RENAME TO "Entry";

CREATE  INDEX "quaint"."ifk_entry_chapter_id" ON "Entry"("chapterId")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Chapter" (
    "dayTime" TEXT NOT NULL DEFAULT 'MORNING' ,
    "id" TEXT NOT NULL  ,
    PRIMARY KEY ("id")
) 

INSERT INTO "quaint"."new_Chapter" ("dayTime", "id") SELECT "dayTime", "id" FROM "quaint"."Chapter"

DROP TABLE "quaint"."Chapter";

ALTER TABLE "quaint"."new_Chapter" RENAME TO "Chapter";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200318164046-revert-some-changes..20200318171606-exclude-dates-for-now
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource DS {
   provider = "sqlite"
-  url = "***"
+  url = env("DATABASE_URL")
 }
 generator photonjs {
   provider = "prisma-client-js"
@@ -16,15 +16,14 @@
 model Entry {
   id          String @id  @default(cuid())
   question    String
   answer      String
-  createdAt   DateTime    @default(now())
-  updatedAt   DateTime    @updatedAt
+  chapterId   Chapter
+
+  @@index([chapterId], name: "ifk_entry_chapter_id")
 }
 model Chapter {
   id          String @id  @default(cuid())
   dayTime     DayTime
   entries     Entry[]
-  createdAt   DateTime    @default(now())
-  updatedAt   DateTime    @updatedAt
 }
```


